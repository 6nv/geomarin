{
  "scripts": {
    "build": "npx webpack --mode production",
    "dev": "npx wepack --mode development --watch",
    "clean": "rm -rf node_modules package-lock.json dist"
  },
  "devDependencies": {
    "webpack-cli": "6.0.1"
  }
}